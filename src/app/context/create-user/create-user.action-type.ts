export enum ActionTypes {
    CREATE_USER = '[CREATE USER] Create a user',
    CREATE_USER_SUCCESS = '[CREATE USER] User creation success',
    CREATE_USER_FAILURE = '[CREATE USER] User creation failed',
  }